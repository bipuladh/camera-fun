{
  "name": "camera-fun",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "cp ./index.html ./dist/index.html && ./node_modules/.bin/esbuild index.ts --minify --bundle --outdir=dist",
    "dev": "cp ./index.html ./dist/index.html && ./node_modules/.bin/esbuild index.ts --bundle --outdir=dist --servedir=dist --serve=3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.15.9",
    "typescript": "^4.8.3"
  },
  "engines": {
    "node": ">=17.0.0"
  }
}
